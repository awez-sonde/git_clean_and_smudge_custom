# Secret sanitization config (never commit the live files)
sanitization.json
!tests/fixtures/sanitization.json
sanitization.learned.json
# Legacy names (still supported by the filter)
local_secrets_map.json
local_secrets_learned.json
.env.mapping
scripts/__pycache__/
tests/fixtures/.sanitization.learned.test.json
tests/__pycache__/

# Local overrides
*.local
.env
.env.local
